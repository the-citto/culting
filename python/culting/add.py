"""Add."""

class Add:
    """Add libraries."""

    def __init__(self, libraries: tuple[str, ...]) -> None:
        """Init."""
        if not libraries:
            raise KeyError



















