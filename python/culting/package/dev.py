"""Dev."""

# from culting.logger import logger
#
# from . import (
#     pkg_python,
# )
#
#
# class Dev:
#     """Dev setup."""
#
#     def __init__(self) -> None:
#         """Init."""
#         logger.debug("setting up dev environment")
#         pkg_python.execute(["-m", "pip", "install", "--upgrade", "pip", "pip-tools"])

# $ ~/.pyenv/versions/3.11.10/bin/python -m venv .venv
# $ .venv/bin/python -m pip install --upgrade pip pip-tools
# $ .venv/bin/python -m piptools compile -o requirements.lock requirements.in --no-strip-extras
# $ .venv/bin/python -m pip install -e .[dev] -c requirements.lock









